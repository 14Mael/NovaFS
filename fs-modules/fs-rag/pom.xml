<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.novafs</groupId>
        <artifactId>fs-modules</artifactId>
        <version>${revision}</version>
    </parent>

    <artifactId>fs-rag</artifactId>
    <packaging>jar</packaging>
    <name>NovaFS :: Modules :: RAG</name>
    <description>RAG 检索增强生成模块:文档解析、向量化、语义检索、问答</description>

    <dependencies>
        <!-- 内部模块 -->
        <dependency>
            <groupId>io.novafs</groupId>
            <artifactId>fs-common-core</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>io.novafs</groupId>
            <artifactId>fs-orm</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>io.novafs</groupId>
            <artifactId>fs-security</artifactId>
            <version>${revision}</version>
        </dependency>

        <!-- 工具库 -->
        <dependency>
            <groupId>cn.hutool</groupId>
            <artifactId>hutool-all</artifactId>
        </dependency>

        <!-- 文档解析(Apache Tika) -->
        <dependency>
            <groupId>org.apache.tika</groupId>
            <artifactId>tika-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.tika</groupId>
            <artifactId>tika-parsers-standard-package</artifactId>
        </dependency>

        <!-- Spring -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- 测试 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>